@page "/dcb"
@using Memoria.Web.Extensibility
@inject DomainTypeRegistry Types

<PageTitle>DCB Overview</PageTitle>

<nav class="breadcrumb" aria-label="Breadcrumb">
    <a href="">Home</a>
    <span aria-hidden="true">/</span>
    <span aria-current="page">DCB</span>
</nav>

<h1>Dynamic consistency boundaries</h1>
<p class="lede">
    The types uploaded through <a href="settings">settings</a> that belong to the DCB model, and
    what they are addressed by.
</p>

@* The counts belong to the types the model declares, and stay on the group rather than on either
   link: they are what the section holds, which is the same fact whichever of the two you open. *@
<ul class="sections">
    <li>
        <div class="group">
            <span class="name">Events</span>
            <span class="detail">@Count(Catalogue.DcbEvents.Count, "event") the model can apply.</span>
            <span class="links">
                <a href="dcb/events/types">Types</a>
                <a href="dcb/events/data">Data</a>
            </span>
        </div>
    </li>
    <li>
        <div class="group">
            <span class="name">Aggregates</span>
            <span class="detail">
                @Count(Catalogue.DcbAggregates.Count, "aggregate"), addressed by
                @Count(Catalogue.DcbAggregateIds.Count, "identifier").
            </span>
            <span class="links">
                <a href="dcb/aggregates/types">Types</a>
                <a href="dcb/aggregates/data">Data</a>
            </span>
        </div>
    </li>
    <li>
        <div class="group">
            <span class="name">Projections</span>
            <span class="detail">
                @Count(Catalogue.DcbProjections.Count, "projection"), addressed by
                @Count(Catalogue.DcbProjectionIds.Count, "identifier").
            </span>
            <span class="links">
                <a href="dcb/projections/types">Types</a>
                <a href="dcb/projections/data">Data</a>
            </span>
        </div>
    </li>
</ul>

@if (Catalogue.Count == 0)
{
    <p class="lede">
        Nothing is registered yet. Upload a zip of assemblies on the
        <a href="settings">settings</a> page.
    </p>
}

@code {

    private DomainTypeCatalogue Catalogue => Types.Current;

    private static string Count(int count, string noun) =>
        count == 1 ? $"1 {noun}" : $"{count} {noun}s";

}
