@page "/"

<PageTitle>Memoria</PageTitle>

<h1>Memoria</h1>
<p class="lede">
    Two models of the same domain, side by side: streams addressed by aggregate, and dynamic
    consistency boundaries addressed by identifier.
</p>

<div class="models">
    <section class="model">
        <h2><a href="streamed">Streamed</a></h2>
        @* The same tiles the rest of the site is entered by, one under the other because a column
           here is half the page rather than the whole of it. Each names a section and goes to it;
           what is under a section — the types the model declares and what has been stored of them —
           is on the page the tile leads to, which is where a reader is asked to choose between
           them rather than twice. *@
        <ul class="tiles">
            <li>
                <a href="streamed">
                    <SectionMark Kind="SectionKind.Overview"/>
                    <span class="body">
                        <span class="name">Overview</span>
                        <span class="detail">What is registered under the streamed model.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="streamed/events">
                    <SectionMark Kind="SectionKind.Events"/>
                    <span class="body">
                        <span class="name">Events</span>
                        <span class="detail">The events the model can apply.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="streamed/aggregates">
                    <SectionMark Kind="SectionKind.Aggregates"/>
                    <span class="body">
                        <span class="name">Aggregates</span>
                        <span class="detail">The write models they are held in.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="streamed/projections">
                    <SectionMark Kind="SectionKind.Projections"/>
                    <span class="body">
                        <span class="name">Projections</span>
                        <span class="detail">The read models built from those events.</span>
                    </span>
                </a>
            </li>
            @* Last of the column, and the one the DCB side has nothing to set against it: a
               boundary there is a query over tags rather than a place events are kept. *@
            <li>
                <a href="streamed/streams">
                    <SectionMark Kind="SectionKind.Streams"/>
                    <span class="body">
                        <span class="name">Streams</span>
                        <span class="detail">The streams the events are held in.</span>
                    </span>
                </a>
            </li>
        </ul>
    </section>

    <section class="model">
        <h2><a href="dcb">DCB</a></h2>
        <ul class="tiles">
            <li>
                <a href="dcb">
                    <SectionMark Kind="SectionKind.Overview"/>
                    <span class="body">
                        <span class="name">Overview</span>
                        <span class="detail">What is registered under the DCB model.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="dcb/events">
                    <SectionMark Kind="SectionKind.Events"/>
                    <span class="body">
                        <span class="name">Events</span>
                        <span class="detail">The events the model can apply.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="dcb/aggregates">
                    <SectionMark Kind="SectionKind.Aggregates"/>
                    <span class="body">
                        <span class="name">Aggregates</span>
                        <span class="detail">The write models they are addressed by.</span>
                    </span>
                </a>
            </li>
            <li>
                <a href="dcb/projections">
                    <SectionMark Kind="SectionKind.Projections"/>
                    <span class="body">
                        <span class="name">Projections</span>
                        <span class="detail">The read models they are addressed by.</span>
                    </span>
                </a>
            </li>
        </ul>
    </section>
</div>
